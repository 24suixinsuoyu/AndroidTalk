<?xml version="1.0" encoding="utf-8"?>
<manifest package="pers.suixinsuoyu.checkpermissionincode"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <permission android:name="pers.suixinsuoyu.checkpermissionincode.permission.SAY_HELLO"></permission>

    <uses-permission android:name="pers.suixinsuoyu.checkpermissionincode.permission.SAY_HELLO">  </uses-permission>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pers.suixinsuoyu.checkpermissionincode.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
